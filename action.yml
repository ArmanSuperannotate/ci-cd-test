name: "CI CD Test Action"
description: "Greets someone"
inputs:
  who-to-greet:
    description: "Who to greet"
    required: true

  sa_token:
    description: "SuperAnnotate access token"
    required: true

  sa_url:
    description: "SuperAnnotate URL"
    required: false

runs:
  using: "node20"
  main: "pipe/pipe.js"
